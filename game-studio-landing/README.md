# 🎮 NEXUS STUDIOS — Landing Page

Современный лендинг для студии разработки игр в hi-tech стиле с интерактивными 3D-эффектами и анимациями.

## ✨ Особенности

- **Particle Flow Background** — интерактивные частицы на Three.js, реагирующие на курсор
- **Glitch Text Effect** — эффект помех при наведении на заголовки
- **Holographic Cards** — 3D-карточки с голографическим свечением
- **GSAP Animations** — плавные scroll-triggered анимации
- **Terminal Loading Screen** — загрузка в стиле консоли
- **Адаптивный дизайн** — mobile-first подход
- **PWA-ready** — работает как нативное приложение

## 🛠 Технологический стек

- **HTML5** — семантическая разметка
- **SCSS** — модульные стили с переменными и миксинами
- **JavaScript (ES6+)** — модульная архитектура
- **Three.js** — 3D частицы и эффекты
- **GSAP + ScrollTrigger** — профессиональные анимации
- **Vite** — быстрая сборка и hot-reload

## 📁 Структура проекта

```
game-studio-landing/
├── index.html              # Главная страница
├── package.json            # Зависимости и скрипты
├── vite.config.js          # Конфигурация Vite
├── public/
│   ├── favicon.svg         # Favicon
│   ├── apple-touch-icon.png
│   └── manifest.json       # PWA manifest
└── src/
    ├── main.js             # Точка входа
    ├── styles/
    │   ├── main.scss       # Главный SCSS файл
    │   ├── variables.scss  # CSS-переменные
    │   ├── mixins.scss     # SCSS миксины
    │   ├── global.scss     # Глобальные стили
    │   ├── components.scss # Стили компонентов
    │   └── animations.scss # Анимации
    └── js/
        ├── loader.js       # Loading анимация
        ├── particles.js    # Particle background (Three.js)
        ├── glitch.js       # Glitch text effect
        ├── hologram.js     # Holographic cards 3D
        ├── scroll.js       # GSAP scroll animations
        └── form.js         # Contact form handler
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 🎨 Цветовая палитра

| Цвет | Hex | Описание |
|------|-----|----------|
| Neon Blue | `#00F0FF` | Основной акцент |
| Purple | `#7B2CBF` | Вторичный акцент |
| Deep Black | `#0A0A0F` | Фон |
| White | `#FFFFFF` | Текст |

## 🎯 Основные секции

1. **Hero** — 3D-логотип, CTA-кнопки, анимированный фон
2. **About** — статистика студии с анимацией чисел
3. **Portfolio** — карточки проектов с 3D-эффектом
4. **Technologies** — стек технологий
5. **Careers** — вакансии
6. **Contacts** — интерактивная форма обратной связи

## 📱 Адаптивность

- **Mobile**: < 768px
- **Tablet**: 768px — 1024px
- **Desktop**: > 1024px

## 🔧 Настройки

### Изменение количества частиц

Откройте `src/js/particles.js` и измените `setParticleCount()`:

```javascript
setParticleCount() {
  const width = window.innerWidth;
  
  if (width < 640) {
    this.particleCount = 500;  // Изменить здесь
  }
  // ...
}
```

### Изменение скорости анимаций

В `src/styles/variables.scss`:

```scss
$transition-fast: 150ms ease;
$transition-base: 300ms ease;
$transition-slow: 500ms ease;
```

## 🌐 SEO Оптимизация

- Семантическая HTML5 разметка
- Meta-теги для социальных сетей (Open Graph, Twitter Cards)
- PWA manifest для установки на устройства
- Оптимизированные шрифты с preload

## 🚀 Деплой на GitHub Pages

### Автоматический деплой (GitHub Actions)

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/USERNAME/REPO_NAME.git
   git push -u origin main
   ```

2. **GitHub Actions автоматически задеплоит сайт** после пуша в ветку `main`

3. **Включите GitHub Pages:**
   - Перейдите в Settings → Pages
   - В разделе "Build and deployment":
     - Source: GitHub Actions
   - После первого деплоя сайт будет доступен по адресу:
     `https://USERNAME.github.io/REPO_NAME/`

### Ручной деплой

```bash
# Сборка
npm run build

# Деплой через gh-pages (требуется пакет)
npm install -D gh-pages
npx gh-pages -d dist
```

### Локальная проверка перед деплоем

```bash
npm run preview
```

## 📊 Производительность

Целевые показатели Lighthouse:
- Performance: 90+
- Accessibility: 90+
- Best Practices: 90+
- SEO: 90+

### Рекомендации для оптимизации:

1. Используйте WebP для изображений
2. Включите gzip/brotli сжатие на сервере
3. Настройте кэширование статики
4. Используйте CDN для шрифтов

## 🔌 Расширение функционала

### Добавление новой секции

1. Добавьте HTML в `index.html`
2. Создайте стили в `src/styles/components.scss`
3. Добавьте анимации в `src/js/scroll.js`

### Добавление новой анимации

В `src/styles/animations.scss`:

```scss
.fade-in-custom {
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 0.6s ease, transform 0.6s ease;
  
  &.visible {
    opacity: 1;
    transform: translateY(0);
  }
}
```

## 📝 Лицензия

MIT

## 👨‍💻 Авторы

NEXUS STUDIOS

---

**Создано с 🚀 для будущих игровых миров**
